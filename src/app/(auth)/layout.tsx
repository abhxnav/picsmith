import { ReactNode } from 'react'

const Layout = ({ children }: { children: ReactNode }) => {
  return (
    <main className="flex-center min-h-screen w-full bg-dark-400">Layout</main>
  )
}

export default Layout
